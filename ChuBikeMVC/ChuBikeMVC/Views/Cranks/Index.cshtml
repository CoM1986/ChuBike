@model IEnumerable<ChuBikeMVC.Models.Parts.Crank>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Part.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Width)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Diameter)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Part.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Width)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Diameter)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CrankId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CrankId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CrankId })
        </td>
    </tr>
}

</table>
